name: GitHub Security Scanner Alert

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Call Postman Github security scanner flow
        run: curl -X GET https://5de49fb59ffa4c948b51e9dd350d7304.flow.pstmn.io
